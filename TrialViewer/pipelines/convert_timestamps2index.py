# This function goes through the data files (provided by Mayo's scripts) and converts
# all of the timestamps into indexes into the video files

# In Unity this then allows you to move through time using just the index of the frames
# instead of having to handle rounding errors with the timestamps